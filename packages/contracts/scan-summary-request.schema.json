{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScanSummaryRequest",
  "description": "Cloud-safe scan summary payload sent from the local agent to the cloud platform.",
  "type": "object",
  "additionalProperties": false,
  "required": ["caseId", "detectedSignals", "riskLevel", "recommendation"],
  "properties": {
    "caseId": {
      "type": "string",
      "minLength": 1
    },
    "detectedSignals": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type", "ruleId", "label", "severity"],
        "properties": {
          "type": {
            "type": "string",
            "minLength": 1
          },
          "ruleId": {
            "type": "string",
            "minLength": 1
          },
          "label": {
            "type": "string",
            "minLength": 1
          },
          "severity": {
            "type": "string",
            "enum": ["LOW", "MEDIUM", "HIGH"]
          }
        }
      }
    },
    "riskLevel": {
      "type": "string",
      "enum": ["LOW", "MEDIUM", "HIGH"]
    },
    "recommendation": {
      "type": "string",
      "enum": ["ALLOW", "WARN", "BLOCK"]
    }
  }
}
